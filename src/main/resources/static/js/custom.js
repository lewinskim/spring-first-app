function myFunction(value){
alert("Button pressesssd" + value)
}

    function add() {
    first=parseInt(firstField.value);
    var second=parseInt(secondField.value);
    var result=first+second;
    result.value=result;
    }